import { Top } from './top';

import Layout from '@/composents/layouts/frame';

export const TopPage = () => {
    return (
        <Layout.Basic>
            <Top />
        </Layout.Basic>
    );
};
